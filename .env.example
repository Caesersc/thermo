# Server
PORT=3000

# Fundraising goal shown on the thermometer/dashboard (USD)
TIP_GOAL=1000

# Ko-fi → your Ko-fi page → Settings → API → Webhooks
# Ko-fi gives you a per-account verification token; every webhook payload
# includes it so you can confirm the request really came from Ko-fi.
KOFI_VERIFY_TOKEN=
